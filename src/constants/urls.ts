const baseURL = 'http://localhost:3001';

const login = '/auth/login';

const register = '/auth/register';

const urls = {
    login: {
        base: login
    },
    register: {
        base: register
    }
}

export {
    baseURL,
    urls
}